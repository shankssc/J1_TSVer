import { light } from '@eva-design/eva';

const appTheme = {
  ...light,
  colors: {
    ...light.colors,
    primary: '#E91E63',    // Primary color (Radiant Pink)
    secondary: '#455A64',  // Secondary color (Deep Grey)
    accent: '#FF5722',     // Accent color (Fiery Orange)
    background: '#212121', // Background color (Charcoal Black)
    text: '#FFFFFF',       // Text color (White)
    // Customize other colors as needed
  },
};

export default appThemeRadiantPink;
