import { light } from '@eva-design/eva';

const appThemeTerracotta = {
  ...light,
  colors: {
    ...light.colors,
    primary: '#E74C3C',    // Primary color (Terracotta)
    secondary: '#EFEFEF',  // Secondary color (Sand Biege)
    accent: '#795548',     // Accent color (Rustic Brown)
    background: '#EFEFEF', // Background color (Light Grey)
    text: '#333333',       // Text color (Dark Grey)
    // Customize other colors as needed
  },
};

export default appThemeTerracotta;