import { light } from '@eva-design/eva';

const appThemeBubblegumPink = {
  ...light,
  colors: {
    ...light.colors,
    primary: '#FF80AB',    // Primary color (Bubblegum Pink)
    secondary: '#AEEEEE',  // Secondary color (Mint green) 
    accent: '#CDDC39',     // Accent color (Lime Green)
    background: '#87CEEB', // Background color (Sky Blue)
    text: '#333333',       // Text color (Dark Grey)
    // Customize other colors as needed
  },
};

export default appThemeBubblegumPink;